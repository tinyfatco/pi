# 2026-07-11 - Pi 0.80.6 upstream rebase

Commits shipped:

- `pi` `a515dbc2d0dca9d455f2b4f61f9b40d2327176f7`
- `pi` `5b9f6df84226d1c0ddefcf6b4bee38b365114f01`
- `pi` `5a8e6a4f9d96c63b87011ad6575cabfd08542857`
- Related Troublemaker upgrade: `49b4e80690589c8fdba9da2c27f00d1a7d4cf821`

Summary:

- Rebased the TinyFat fork from Pi 0.75.5-era integration work onto upstream 0.80.6.
- Preserved the TinyFat model session-mode metadata extension across the upstream model-catalog refactor.
- Dropped the old custom Fireworks GLM 5.2 catalog patch because upstream now provides GLM 5.2 and GLM 5.2 Fast natively.
- Made Ollama integration tests explicitly opt-in with `PI_TEST_LOCAL_LLM=1`, require `gpt-oss:20b` to be installed already, and removed automatic model downloads from tests.
- Preserved the pre-rebase fork at `codex/pre-pi-0.80.6-rebase-20260711` and the prior hidden local `AGENTS.md` state at `codex/save-local-pi-agents-20260711`.

Verification:

- `npm ci`
- `npm run build`
- `npm test`: agent 180 passed; AI 531 passed and 696 skipped; coding-agent 1,535 passed and 47 skipped; TUI passed.
- `npm run check` via the pre-commit hook.
- Confirmed the default test run left `~/.ollama` at 8 KB and did not download a model.

Deploy status:

- Fork `main` pushed to `tinyfatco/pi` with force-with-lease after the requested rebase.
- No package publish or production deploy was performed.

Manual QA gaps:

- Ollama tests were intentionally not run because they require an existing 13 GB `gpt-oss:20b` installation and explicit opt-in.
